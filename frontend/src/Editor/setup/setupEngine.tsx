
import { DataflowEngine, ControlFlowEngine } from "rete-engine";
import { Editor } from "..";
import { Schemes } from "../presets/types";

export function setupEngine() {
    const { editor } = Editor.get();
} 